machine:
  node:
    version: 0.12.0

dependencies:
  pre:
    - npm install -g typescript

test:
  pre:
    - npm run clean
    - npm run compile
  post:
    - $(npm bin)/coveralls < ./coverage/lcov.info

general:
  branches:
    ignore:
      - gh-pages
      - /release\/.*/
